project.ext {
}

subprojects {

    if (file('src/main/java').exists()) {
        apply plugin: 'jacoco'
        apply plugin: 'java'

        repositories {
            jcenter()
        }

        dependencies {
            compile (
			)
            testCompile (
			)
        }

        jacoco {
            toolVersion = version_jacoco
        }

        compileJava {
            sourceCompatibility = version_java
            targetCompatibility = version_java

            options.fork = true
        }

        test {
            jacoco {
                append = false
                includes = [
                        "com/github/matthiasbalke/**/*"
                ]
            }
        }

    } else {
        apply plugin: 'base'
    }

}
